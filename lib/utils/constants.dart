import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFFF97316); // Warm Orange
  static const primaryDark = Color(0xFFEA580C); // Darker Warm Orange
  static const secondary = Color(0xFFF59E0B); // Amber
  static const accent = Color(0xFFFB923C); // Light Orange
  static const background = Color(0xFFFFF7ED); // Very Light Cream/Orange
  static const surface = Colors.white;
  static const error = Color(0xFFEF4444);
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const info = Color(0xFF3B82F6);
  static const textDark = Color(0xFF431407); // Deep Warm Brown
  static const textMedium = Color(0xFF78350F); // Warm Brown
  static const textLight = Color(0xFFA87E6E); // Muted Warm Gray
  static const divider = Color(0xFFFFEDD5); // Soft Orange Divider
  static const border = Color(0xFFFED7AA); // Light Orange Border
  static const cardShadow = Color(0x0C431407); // Warm Tinted Shadow
}

class AppStrings {
  static const appName = 'Axorvian';
  static const tagline = 'YOUR WORLD OF STYLE';
  static const baseUrl = 'https://dummyjson.com';
}

class AppSizes {
  static const radiusSm = 8.0;
  static const radiusMd = 16.0;
  static const radiusLg = 24.0;
  static const radiusXl = 32.0;
  static const paddingSm = 8.0;
  static const paddingMd = 16.0;
  static const paddingLg = 24.0;
}